# GenLayer faucet account private key (server-side only — NEVER expose to the browser).
GENLAYER_PRIVATE_KEY=0x...

# Deployed RugProof intelligent contract address (filled in after `npm run deploy:contract`).
CONTRACT_ADDRESS=0x...

# Same address, exposed to the browser for the EVM wallet adapter.
NEXT_PUBLIC_CONTRACT_ADDRESS=0x...
